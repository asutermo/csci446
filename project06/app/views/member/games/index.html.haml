%p#notice= notice
%p#alert= alert
%h2
  My Gamez
= link_to 'add a new game', new_member_game_path
%p
  = will_paginate @games
%table
  %tr
    %th.title Title
    %th.rating Rating
    %th.date Date Added
    %th.buttons
  - @games.each do |game|
    %tr
      %td.title= g_title(game)
      %td.rating= g_rating(game)
      %td.date= g_date(game) 
%br/
%p
  = "Created #{pluralize(@games.count, 'game')}, #{percent} rated."
