%p#alert= alert
%p
  = will_paginate @games
%game_list
  %tr
    %th.title Title
    %th.rating Rating
    %th.date Date Added
  - @games.each do |game|
    %tr
      %td.title= g_title(game)
      %td.rating= g_rating(game)
      %td.date= g_date(game)